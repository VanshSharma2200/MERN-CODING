import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';  // Create this file if it's missing, or remove this line if you don't need it
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
